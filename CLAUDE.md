# 박재상 의 AX Lab 저장소 — CLAUDE.md

> 이 파일은 Claude가 이 폴더를 열 때마다 자동으로 읽는 메모입니다. 사람도 Claude도 여기 적힌 대로 일합니다.

## 나는 누구인가
- 박재상 · 보이스루 Toon Japan. 개발을 배우지 않은 실무자이며 **코드를 읽지 않습니다.**
- 이 저장소에서 풀려는 업무: 툰식이 고도화 — 원본 재수급 이후 툰식이가 원본 자동 이관 + 원본 수급 후 자동 이관
- 주로 다루는 자료: 구글 시트 · 슬랙 · 사내 시스템 (Kuaikan 등)

## Claude 가 지킬 규칙
- **항상 한국어, 쉬운 말.** 전문용어로 질문하지 않는다. 선택지는 2~3개로 준다.
- **만들기 전에 예고** — 무엇을 왜 바꿀지 1~2줄 먼저. 끝나면 "무엇이 됐는지 + 어떻게 확인하는지".
- **되돌리기 어려운 일은 먼저 확인** — 파일 삭제·전체 덮어쓰기, 그리고 슬랙 발송·시트 수정·메일 발송 같은 **첫 외부 쓰기**는 미리보기 후 승인받고 실행.
- **비밀값(토큰·API 키·서비스계정 JSON)은 코드나 채팅에 넣지 않는다.** 문자열은 `.env`, 파일은 `secrets/` 에 두고 둘 다 `.gitignore` 로 제외돼 있다.
- **오류는 원인 1줄 + 해결 1줄**로. 에러 원문·스택트레이스를 그대로 보여주지 않는다.
- **확실하지 않으면 추측하지 말고 묻는다.**

## 이 저장소의 문서 (docs/)
- `docs/PRD.md` — 무엇을 만들지 한 장 (`/ax-prd`)
- `docs/BASELINE.md` — 지금 이 업무에 드는 시간·건수 실측 (`/ax-baseline`)
- `docs/ASSET_CHECK.md` — 이미 사내에 있는지 점검 (`/ax-check`)
- `docs/REPORTS.md` — 주간 보고 (`/ax-report`)
- 작업이 끝나면 `/ax-save` 로 GitHub 에 올린다.

## 프로젝트 메모
- 브랜치 `ax-lab`: 이 환경(로컬 + AX Lab 세션)에서만 하는 작업을 모아두는 브랜치. dev로 머지 후 main으로 반영.
- 작품명은 사내 표기 기준을 따른다.
- 날짜는 YYYY-MM-DD 형식 사용.
