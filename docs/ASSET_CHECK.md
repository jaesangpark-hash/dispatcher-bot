# 기존 자산 점검 — 툰식이를 통한 원본 파일 이관 (재수급·수동 요청)

작성: 2026-09-08

## 업무 분해
입력: 원본 재수급 완료 알림(자동) 또는 슬랙 명령(수동) → 처리: Kuaikan 드라이브에서 해당 회차 PSD 다운로드 → PIVO 업로드 → 전처리 대기 → 출력: 슬랙에 이관 완료/실패 메시지

## 점검 결과
| 갈래 | 확인 내용 | 상태 |
|---|---|---|
| 옆자리 동료 | 별도 매크로·스크립트 없음 | 없음 |
| 사내 시스템 | CFM 봇(AIA 팀): 최초 이관 전용. 재수급·수동 명령 용도에 맞지 않고 활용 번거로움. 툰식이에 기능 있으나 미완성 | 있음(CFM 봇) — 이번 용도 부적합 |
| 이전 기수·AIA | CFM 봇 = AIA 팀봇. 위와 동일 | 있음 — 이번 용도 부적합 |
| 외부 도구 | 구글 시트/슬랙 워크플로 불필요. 툰식이로 처리 | 불필요 |
| 데이터 출처 | 구글 시트에 작품·PIVO ID 정리됨. Kuaikan 폴더·파일 경로 탐색 로직 이미 구현됨 | 있음(시트 + 기존 로직) |

## 결론
**C. 빈틈만 채운다** — 근거 1: 핵심 로직(Kuaikan 검색·다운로드, PIVO 업로드, 경로 탐색)이 툰식이에 이미 구현돼 있고 데이터 출처도 연결됨 / 근거 2: CFM 봇은 최초 이관 전용이라 재수급에 부적합하며 외부 도구 대체 이유 없음 — 재수급 자동 트리거 + 명령 안정화(오류 처리·중복 방지)만 완성하면 됨

## 아직 모르는 것 (채널에 물어볼 것)
- 없음
